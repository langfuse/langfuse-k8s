# Langfuse Helm Chart Configuration

# -- Override the name for the selector labels, defaults to the chart name
nameOverride: ""

# -- Override the full name of the deployed resources, defaults to a combination of the release name and the name for the selector labels
fullnameOverride: ""

# Core Langfuse Configuration
langfuse:
  # Logging configuration
  logging:
    # -- Set the log level for the application (trace, debug, info, warn, error, fatal)
    level: info
    # -- Set the log format for the application (text or json)
    format: text

  # -- Salt used to hash API keys. Leave empty to auto-generate on first install (persisted in `<release>-app`); override with value/secretKeyRef to pin. Generate via `openssl rand -base64 32`.
  salt:
    value: ""
    secretKeyRef:
      name: ""
      key: ""

  # -- Langfuse EE license key.
  licenseKey:
    value: ""
    secretKeyRef:
      name: ""
      key: ""

  # -- EE: Langfuse allowed organization creators. See [documentation](https://langfuse.com/self-hosting/organization-creators)
  allowedOrganizationCreators: []

  # Authentication and SSO settings. See https://langfuse.com/self-hosting/authentication-and-sso
  # Set auth.disableUsernamePassword to true to disable username/password login. Provider options use
  # the last part of the env var name in camelCase under auth.providers; prefer secretKeyRef for secrets.
  # Example (Azure AD):
  # auth:
  #   disableUsernamePassword: true
  #   providers:
  #     azureAd:
  #       clientId: "<YOUR CLIENT ID>"
  #       clientSecret:
  #         secretKeyRef:
  #           name: "my-sso-secret"
  #           key: "azure-client-secret"
  #       tenantId: "<YOUR TENANT ID>"

  # -- Encryption key for sensitive data (256 bits / 64 hex chars). Leave empty to auto-generate on first install (persisted in `<release>-app`); override with value/secretKeyRef to pin. Generate via `openssl rand -hex 32`.
  encryptionKey:
    value: ""
    secretKeyRef:
      name: ""
      key: ""

  # Feature flags
  features:
    # -- Whether or not to report basic usage statistics to a centralized server.
    telemetryEnabled: true
    # -- Disable public sign up
    signUpDisabled: false
    # -- Enable experimental features
    experimentalFeaturesEnabled: false

  # -- Node.js environment to use for all langfuse deployments
  nodeEnv: production

  serviceAccount:
    # -- Whether to create a service account for all langfuse deployments
    create: true
    # -- Annotations for the service account
    annotations: {}
    # -- Override the name of the service account to use, discovered automatically if not set
    name: ""
    # -- Whether to automount service account token in pods. Set to false to disable automatic mounting of the service account token.
    automountServiceAccountToken: true

  smtp:
    # -- SMTP connection URL. See [documentation](https://langfuse.com/self-hosting/transactional-emails)
    connectionUrl: ""
    # -- From address for emails. Required if connectionUrl is set.
    fromAddress: ""

  ingress:
    # -- Set to `true` to enable the ingress resource
    enabled: false
    # -- Additional labels for the ingress resource
    additionalLabels: {}
    # -- Annotations for the ingress resource
    annotations: {}
    # -- The class name for the ingress resource
    className: ""
    # -- The hosts for the ingress resource
    hosts: []
    # Example:
    # - host: "langfuse.example.com"
    #   paths:
    #     - path: /
    #       pathType: ImplementationSpecific

    tls:
      # -- Set to `true` to enable use HTTPS on the ingress
      enabled: false
      # -- The name of the secret to use for TLS Key
      secretName: ""

  # Kubernetes Gateway API. Alternative to `langfuse.ingress`; requires the
  # Gateway API CRDs and a Gateway that this route attaches to.
  httpRoute:
    # -- Set to `true` to enable the Gateway API HTTPRoute resource
    enabled: false
    # -- Set to `false` to skip the Gateway API CRD preflight check (offline rendering, GitOps diff)
    crdCheck: true
    # -- Additional labels for the HTTPRoute resource
    additionalLabels: {}
    # -- Annotations for the HTTPRoute resource
    annotations: {}
    # -- API version of the HTTPRoute resource. Auto-detected from the cluster when empty
    apiVersion: ""
    # -- The Gateways this route attaches to. Required when `enabled` is `true`
    parentRefs: []
    # Example:
    # - name: my-gateway
    #   namespace: gateway-system
    #   sectionName: https

    # -- The hostnames matched by this route
    hostnames: []
    # Example:
    # - "langfuse.example.com"

    # -- Routing rules. Rules without `backendRefs` are pointed at the langfuse web service. Defaults to a single `/` prefix rule when empty
    rules: []
    # Example:
    # - matches:
    #     - path:
    #         type: PathPrefix
    #         value: /
    #   timeouts:
    #     request: 60s

  # -- Pod security context for all langfuse deployments
  podSecurityContext: {}
  # -- Security context for all langfuse deployments
  securityContext: {}
  # -- Node selector for all langfuse deployments
  nodeSelector: {}
  # -- Tolerations for all langfuse deployments
  tolerations: []
  # -- Affinity for all langfuse deployments
  affinity: {}
  # -- PriorityClass for all langfuse deployments
  priorityClassName: ""
  # -- DNS configuration for all langfuse deployments
  dnsConfig: {}
  pod:
    # -- Annotations for all langfuse pods
    annotations: {}
    # -- Labels for all langfuse pods
    labels: {}
    # -- Topology spread constraints for all langfuse pods
    topologySpreadConstraints: []
  deployment:
    # -- Annotations for all langfuse deployments
    annotations: {}
    # -- Deployment strategy for all langfuse deployments (can be overridden by individual deployments)
    strategy: {}
  # -- Number of replicas to use for all langfuse deployments. Can be overridden by the individual deployments
  replicas: 1
  # -- Number of old ReplicaSets to retain to allow rollback. Can be overridden by the individual deployments
  revisionHistoryLimit: 10
  # -- Resources for all langfuse deployments. Can be overridden by the individual deployments
  resources: {}
  image:
    # -- The image tag to use for all langfuse deployments. Can be overridden by the individual deployments. Falls back to appVersion if not set.
    tag: Null
    # -- The pull policy to use for all langfuse deployments. Can be overridden by the individual deployments.
    pullPolicy: Always
    # -- The pull secrets to use for all langfuse deployments. Can be overridden by the individual deployments.
    pullSecrets: []

  # -- List of additional environment variables to be added to all langfuse deployments. See [documentation](https://langfuse.com/docs/deployment/self-host#configuring-environment-variables) for details.
  additionalEnv: []
  # -- Secrets or ConfigMap of additional environment variables to be added to all langfuse deployments. See [documentation](https://langfuse.com/docs/deployment/self-host#configuring-environment-variables) for details.
  additionalEnvFrom: []
  # -- Allows additional containers to be added to all langfuse deployments
  extraContainers: []
  # -- Allows additional volumes to be added to all langfuse deployments
  extraVolumes: []
  # -- Allows additional init containers to be added to all langfuse deployments
  extraInitContainers: []
  # -- Allows additional volume mounts to be added to all langfuse deployments
  extraVolumeMounts: []

  # -- Allows additional lifecycle hooks to be added to all langfuse deployments
  extraLifecycle: {}

  # -- Langfuse AI features: the in-app agent and Ask AI. One instance-wide Langfuse AI model powers both. Requires application version >= 4.24. Helm does not create AWS Lambda MicroVM resources; pass ARNs from Terraform or a manual AWS setup. See https://langfuse.com/self-hosting/configuration/langfuse-assistant
  aiFeatures:
    # -- LANGFUSE_AI_PROVIDER: bedrock, anthropic, or openai. Required to enable the AI features; there is no default. Must be set together with model.
    provider: ""
    # -- LANGFUSE_AI_MODEL. Required whenever provider is set.
    model: ""
    # -- LANGFUSE_AI_SMALL_MODEL for supplementary calls such as conversation titles
    smallModel: ""
    # -- LANGFUSE_AI_API_KEY. Required for anthropic and openai; rejected for bedrock, which authenticates through the AWS credential chain. Prefer secretKeyRef.
    apiKey:
      value: ""
      secretKeyRef:
        name: ""
        key: ""
    # -- LANGFUSE_AI_BASE_URL. For openai, include `/v1`.
    baseUrl: ""
    # -- LANGFUSE_AI_EXTRA_HEADERS as a JSON object string
    extraHeaders: ""
    # -- Set LANGFUSE_AI_USE_RESPONSES_API=true for the OpenAI Responses API
    useResponsesApi: false
    # -- LANGFUSE_AI_AWS_BEDROCK_REGION
    bedrockRegion: ""
    # -- LANGFUSE_AI_FEATURES_PROJECT_ID for tracing AI feature runs on this instance
    projectId: ""
    inAppAgent:
      # -- Set to `true` to set LANGFUSE_IN_APP_AGENT_ENABLED on web and worker
      enabled: false
      mcp:
        # -- Point the worker's MCP calls at the in-cluster web Service and allow that Host on web. Sets LANGFUSE_MCP_BASE_URL, so the externally resolvable NEXTAUTH_URL is left alone; the worker also builds email and Slack links from it. Requires application version >= 4.25, unlike the rest of this block; on 4.24 the worker ignores it and falls back to NEXTAUTH_URL.
        useInternalWebUrl: false
      sandbox:
        # -- LANGFUSE_IN_APP_AGENT_SANDBOX_PROVIDER. `lambda-microvm` is the only accepted value; leave empty for Langfuse tools only. The application also has a development-only `dangerous-docker` provider, which is not usable in a deployed instance.
        provider: ""
        # -- LANGFUSE_IN_APP_AGENT_SANDBOX_AWS_LAMBDA_MICROVM_IMAGE_IDENTIFIER
        imageIdentifier: ""
        # -- LANGFUSE_IN_APP_AGENT_SANDBOX_AWS_LAMBDA_MICROVM_EXECUTION_ROLE_ARN
        executionRoleArn: ""
        # -- LANGFUSE_IN_APP_AGENT_SANDBOX_AWS_LAMBDA_MICROVM_REGION
        region: ""
        # -- LANGFUSE_IN_APP_AGENT_SANDBOX_AWS_LAMBDA_MICROVM_EGRESS_NETWORK_CONNECTOR_ARN. Required with the lambda-microvm provider: without it AWS attaches its default INTERNET_EGRESS connector and sandboxed code reaches the public internet.
        egressNetworkConnectorArn: ""

  # Web deployment configuration
  web:
    image:
      # -- The image repository to use for the langfuse web pods.
      repository: docker.langfuse.com/langfuse/langfuse
      # -- The tag to use for the langfuse web pods. Using `langfuse.image.tag` if not set.
      tag: Null
      # -- The pull policy to use for the langfuse web pods. Using `langfuse.image.pullPolicy` if not set.
      pullPolicy: Null
      # -- The pull secrets to use for the langfuse web pods. Using `langfuse.image.pullSecrets` if not set.
      pullSecrets: Null
    deployment:
      # -- Additional labels for the langfuse web deployment
      additionalLabels: {}
      # -- Annotations for the web deployment
      annotations: {}
      # -- Deployment strategy for the web deployment. Overrides the global deployment strategy
      strategy: {}
    pod:
      # -- Annotations for the web pods
      annotations: {}
      # -- Labels for the web pods
      labels: {}
      # -- Node selector for the web pods. Overrides the global nodeSelector
      nodeSelector: null
      # -- Tolerations for the web pods. Overrides the global tolerations
      tolerations: null
      # -- Affinity for the web pods. Overrides the global affinity
      affinity: null
      # -- Topology spread constraints for the web pods. Overrides the global topologySpreadConstraints
      topologySpreadConstraints: null
      # -- PriorityClass for the web pods. Overrides the global priorityClassName
      priorityClassName: null
      # -- Allows additional containers to be added to all langfuse web pods
      extraContainers: []
      # -- List of additional environment variables to be added to all langfuse web pods. See [documentation](https://langfuse.com/docs/deployment/self-host#configuring-environment-variables) for details.
      additionalEnv: []
      # -- Secrets or ConfigMap of additional environment variables to be added to all langfuse web pods. See [documentation](https://langfuse.com/docs/deployment/self-host#configuring-environment-variables) for details.
      additionalEnvFrom: []
    service:
      # -- The type of service to use for the langfuse web application
      type: ClusterIP
      # -- The port to use for the langfuse web application
      port: 3000
      # -- The external port that will be exposed by the service. Falls back to `port` if not set.
      externalPort: null
      # -- The node port to use for the langfuse web application
      nodePort: null
      # -- Additional labels for the langfuse web application service
      additionalLabels: {}
      # -- Annotations for the langfuse web application service
      annotations: {}

    # -- Resources for the langfuse web pods. Defaults to the global resources
    resources: {}

    # -- Number of replicas to use if HPA is not enabled. Defaults to the global replicas
    replicas: null

    # -- Number of old ReplicaSets to retain to allow rollback.
    revisionHistoryLimit: null

    # KEDA ScaledObject configuration
    keda:
      # -- Set to `true` to enable KEDA for the langfuse web pods
      # Note: When both KEDA and HPA are enabled, the deployment will fail.
      enabled: false
      # -- The minimum number of replicas to use for the langfuse web pods
      minReplicas: 1
      # -- The maximum number of replicas to use for the langfuse web pods
      maxReplicas: 2
      # -- The polling interval in seconds for checking metrics
      pollingInterval: 30
      # -- The trigger type for scaling (cpu or memory)
      triggerType: "cpu"
      # -- The metric type for scaling (Utilization or AverageValue)
      metricType: "Utilization"
      # -- The target utilization percentage for the langfuse web pods
      value: "50"
      # -- Optional container name to target for metrics (leave empty to target all containers)
      containerName: ""

    # Horizontal Pod Autoscaler configuration
    hpa:
      # -- Set to `true` to enable HPA for the langfuse web pods
      # Note: When both KEDA and HPA are enabled, the deployment will fail.
      enabled: false
      # -- Annotations for the langfuse web HPA
      annotations: {}
      # -- The minimum number of replicas to use for the langfuse web pods
      minReplicas: 1
      # -- The maximum number of replicas to use for the langfuse web pods
      maxReplicas: 2
      # -- The target CPU utilization percentage for the langfuse web pods
      targetCPUUtilizationPercentage: 50

    # Vertical Pod Autoscaler configuration
    vpa:
      # -- Set to `true` to enable VPA for the langfuse web pods
      enabled: false
      # -- The resources to control for the langfuse web pods
      controlledResources: []
      # -- The maximum allowed resources for the langfuse web pods
      maxAllowed: {}
      # -- The minimum allowed resources for the langfuse web pods
      minAllowed: {}
      updatePolicy:
        # -- The update policy mode for the langfuse web pods
        updateMode: Auto

    # -- Adding records to /etc/hosts in the pod's network.
    hostAliases: []

    livenessProbe:
      # -- Path to check for liveness.
      path: "/api/public/health"
      # -- Initial delay seconds for livenessProbe.
      initialDelaySeconds: 20
      # -- Period seconds for livenessProbe.
      periodSeconds: 10
      # -- Timeout seconds for livenessProbe.
      timeoutSeconds: 5
      # -- Failure threshold for livenessProbe.
      failureThreshold: 3
      # -- Success threshold for livenessProbe.
      successThreshold: 1

    readinessProbe:
      # -- Path to check for readiness.
      path: "/api/public/ready"
      # -- Initial delay seconds for readinessProbe.
      initialDelaySeconds: 20
      # -- Period seconds for readinessProbe.
      periodSeconds: 10
      # -- Timeout seconds for readinessProbe.
      timeoutSeconds: 5
      # -- Failure threshold for readinessProbe.
      failureThreshold: 3
      # -- Success threshold for readinessProbe.
      successThreshold: 1

    # Pod Disruption Budget configuration for the web deployment
    pdb:
      # -- Set to `true` to create a Pod Disruption Budget for the langfuse web pods
      create: true
      # -- Minimum number of available pods during disruptions. Cannot be set simultaneously with maxUnavailable.
      minAvailable: ""
      # -- Maximum number of unavailable pods during disruptions. Cannot be set simultaneously with minAvailable. Defaults to 1 if neither is set.
      maxUnavailable: ""

  # Worker deployment configuration
  worker:
    # -- Adding records to /etc/hosts in the worker pod's network.
    hostAliases: []
    image:
      # -- The image repository to use for the langfuse worker pods
      repository: docker.langfuse.com/langfuse/langfuse-worker
      # -- The tag to use for the langfuse worker pods. Using `langfuse.image.tag` if not set.
      tag: Null
      # -- The pull policy to use for the langfuse worker pods. Using `langfuse.image.pullPolicy` if not set.
      pullPolicy: Null
      # -- The pull secrets to use for the langfuse worker pods. Using `langfuse.image.pullSecrets` if not set.
      pullSecrets: Null
    deployment:
      # -- Additional labels for the worker deployment
      additionalLabels: {}
      # -- Annotations for the worker deployment
      annotations: {}
      # -- Deployment strategy for the worker deployment. Overrides the global deployment strategy
      strategy: {}
    pod:
      # -- Annotations for the worker pods
      annotations: {}
      # -- Labels for the worker pods
      labels: {}
      # -- Node selector for the worker pods. Overrides the global nodeSelector
      nodeSelector: null
      # -- Tolerations for the worker pods. Overrides the global tolerations
      tolerations: null
      # -- Affinity for the worker pods. Overrides the global affinity
      affinity: null
      # -- Topology spread constraints for the worker pods. Overrides the global topologySpreadConstraints
      topologySpreadConstraints: null
      # -- PriorityClass for the worker pods. Overrides the global priorityClassName
      priorityClassName: null
      # -- Allows additional containers to be added to all langfuse worker pods
      extraContainers: []
      # -- List of additional environment variables to be added to all langfuse worker pods. See [documentation](https://langfuse.com/docs/deployment/self-host#configuring-environment-variables) for details.
      additionalEnv: []
      # -- Secrets or ConfigMap of additional environment variables to be added to all langfuse worker pods. See [documentation](https://langfuse.com/docs/deployment/self-host#configuring-environment-variables) for details.
      additionalEnvFrom: []
    # -- Resources for the langfuse worker pods. Defaults to the global resources
    resources: {}

    # -- Number of replicas to use if HPA is not enabled. Defaults to the global replicas
    replicas: null

    # -- Number of old ReplicaSets to retain to allow rollback.
    revisionHistoryLimit: null

    # KEDA ScaledObject configuration
    keda:
      # -- Set to `true` to enable KEDA for the langfuse worker pods
      # Note: When both KEDA and HPA are enabled, the deployment will fail.
      enabled: false
      # -- The minimum number of replicas to use for the langfuse worker pods
      minReplicas: 1
      # -- The maximum number of replicas to use for the langfuse worker pods
      maxReplicas: 2
      # -- The polling interval in seconds for checking metrics
      pollingInterval: 30
      # -- The trigger type for scaling (cpu or memory)
      triggerType: "cpu"
      # -- The metric type for scaling (Utilization or AverageValue)
      metricType: "Utilization"
      # -- The target utilization percentage for the langfuse worker pods
      value: "50"
      # -- Optional container name to target for metrics (leave empty to target all containers)
      containerName: ""

    # Horizontal Pod Autoscaler configuration
    hpa:
      # -- Set to `true` to enable HPA for the langfuse worker pods
      # Note: When both KEDA and HPA are enabled, the deployment will fail.
      enabled: false
      # -- Annotations for the langfuse worker HPA
      annotations: {}
      # -- The minimum number of replicas to use for the langfuse worker pods
      minReplicas: 1
      # -- The maximum number of replicas to use for the langfuse worker pods
      maxReplicas: 2
      # -- The target CPU utilization percentage for the langfuse worker pods
      targetCPUUtilizationPercentage: 50

    # Vertical Pod Autoscaler configuration
    vpa:
      # -- Set to `true` to enable VPA for the langfuse worker pods
      enabled: false
      # -- The resources to control for the langfuse worker pods
      controlledResources: []
      # -- The maximum allowed resources for the langfuse worker pods
      maxAllowed: {}
      # -- The minimum allowed resources for the langfuse worker pods
      minAllowed: {}
      updatePolicy:
        # -- The update policy mode for the langfuse worker pods
        updateMode: Auto

    # Pod Disruption Budget configuration for the worker deployment
    pdb:
      # -- Set to `true` to create a Pod Disruption Budget for the worker deployment
      create: true
      # -- Minimum number of available pods during disruptions. Cannot be set simultaneously with maxUnavailable.
      minAvailable: ""
      # -- Maximum number of unavailable pods during disruptions. Cannot be set simultaneously with minAvailable. Defaults to 1 if neither is set.
      maxUnavailable: ""

    livenessProbe:
      # -- Initial delay seconds for livenessProbe.
      initialDelaySeconds: 20
      # -- Period seconds for livenessProbe.
      periodSeconds: 10
      # -- Timeout seconds for livenessProbe.
      timeoutSeconds: 5
      # -- Failure threshold for livenessProbe.
      failureThreshold: 3
      # -- Success threshold for livenessProbe.
      successThreshold: 1

  # -- Allow helm upgrade that would replace leftover v1 Bitnami stores (CH STS, PG PVC, MinIO Deploy) with empty v2 volumes. Default false. The supported path is examples/upgrade-v1-to-v2 (in-place only when stores are external; otherwise a sibling v2 release).
  allowV1Upgrade: false

  # NextAuth configuration
  nextauth:
    # -- When deploying to production, set the `nextauth.url` value to the canonical URL of your site.
    url: http://localhost:3000
    # -- NextAuth secret for JWT encryption and email token hashing. Leave empty to auto-generate on first install (persisted in `<release>-app`); override with value/secretKeyRef to pin.
    secret:
      value: ""
      secretKeyRef:
        name: ""
        key: ""

# PostgreSQL Configuration
# Deploys a single-instance PostgreSQL via the groundhog2k/postgres sub-chart (image `docker.io/postgres`).
# For HA / managed Postgres, set `postgresql.deploy: false` and configure `host` + auth fields below.
postgresql:
  # -- Deploy PostgreSQL via the bundled groundhog2k/postgres sub-chart. Disable to use an external or managed Postgres.
  deploy: true

  # -- PostgreSQL host to connect to. If postgresql.deploy is true, this will be set automatically based on the release name.
  host: ""
  # -- Port of the postgres server to use. Defaults to 5432.
  port: null
  # -- Additional database connection arguments
  args: ""

  # -- If `postgresql.deploy` is set to false, Connection string of your Postgres database used for database migrations. Use this if you want to use a different user for migrations or use connection pooling on DATABASE_URL. For large deployments, configure the database user with long timeouts as migrations might need a while to complete.
  directUrl: ""

  # -- If your database user lacks the CREATE DATABASE permission, you must create a shadow database and configure the "SHADOW_DATABASE_URL". This is often the case if you use a Cloud database. Refer to the Prisma docs for detailed instructions.
  shadowDatabaseUrl: ""

  # Authentication configuration (Langfuse-facing — these values are read by the chart's helpers
  # to build DATABASE_USERNAME / DATABASE_PASSWORD / DATABASE_NAME for the langfuse pods).
  auth:
    # -- Username Langfuse uses to connect to PostgreSQL. Created automatically by the sub-chart when deploy is true.
    username: langfuse
    # -- Password Langfuse uses to connect to PostgreSQL. Leave empty to have the chart generate one (stored in a release-managed Secret).
    password: ""
    # -- If you want to use an existing secret for the postgres password, set the name of the secret here. (`postgresql.auth.password` will be ignored and picked up from this secret).
    existingSecret: ""
    # -- Keys in existingSecret: userPasswordKey for Langfuse; adminPasswordKey for the Postgres superuser when deploy is true.
    secretKeys:
      userPasswordKey: password
      adminPasswordKey: postgres-password
    # -- Database name Langfuse uses. Created automatically by the sub-chart when deploy is true.
    database: langfuse
    # -- Additional database connection arguments
    args: ""

  # Migration configuration
  migration:
    # -- Whether to run automatic migrations on startup
    autoMigrate: true

  # Sub-chart settings (groundhog2k/postgres) — passed through to the sub-chart.
  # See https://artifacthub.io/packages/helm/groundhog2k/postgres for the full schema.
  image:
    # -- Postgres image. Defaults to the upstream Docker Hub image (Apache 2.0 / PostgreSQL license).
    repository: postgres
    tag: "18"
    pullPolicy: IfNotPresent

  # -- Number of replicas for the Postgres StatefulSet. Single-instance only — set to 0 to suspend.
  replicaCount: 1

  service:
    type: ClusterIP
    # -- Port the Postgres Service exposes. Must match `postgresql.port` (or the langfuse default 5432).
    port: 5432

  # -- PVC settings for the Postgres data volume.
  storage:
    # Size of the data PVC. Adjust based on expected trace + project volume.
    requestedSize: 20Gi
    # -- StorageClass name. Leave empty to use the cluster's default.
    className: ""
    # -- Keep the PVC after the release is uninstalled.
    persistentVolumeClaimRetentionPolicy:
      whenDeleted: Retain
      whenScaled: Retain

  # -- Pass-through Postgres settings for the groundhog2k sub-chart. Usually left alone; helpers wire the chart-managed Secret automatically.
  settings:
    # -- Postgres superuser password. Auto-derived from the chart-managed Secret unless existingSecret is set.
    superuserPassword: {}
    # -- Existing Secret for the superuser password (keys: POSTGRES_USER, POSTGRES_PASSWORD). Single source of truth for the chart-managed Secret's name — the chart creates the Secret under this exact name, so any release name works. Override it (together with userDatabase.existingSecret) when running two releases in one namespace.
    existingSecret: "langfuse-postgresql-auth"

  # -- Bootstrap the Langfuse database/user on first start. Empty name/user/password maps use defaults from auth.* via the chart-managed Secret.
  userDatabase:
    name: {}
    user: {}
    password: {}
    # -- Existing Secret for userDatabase credentials (keys: USERDB_USER, USERDB_PASSWORD, POSTGRES_DB). Must match settings.existingSecret when the chart manages the Secret (validated at render time).
    existingSecret: "langfuse-postgresql-auth"

  # -- Resource limits/requests for the Postgres pod. Tune for your workload.
  resources: {}
    # limits:
    #   cpu: 2000m
    #   memory: 2Gi
    # requests:
    #   cpu: 500m
    #   memory: 512Mi

  # -- Node selector / tolerations / affinity for the Postgres pod.
  nodeSelector: {}
  tolerations: []
  affinity: {}

  # -- Pod security context.
  podSecurityContext:
    fsGroup: 999
  # -- Container security context.
  securityContext:
    runAsUser: 999
    runAsGroup: 999

  # -- Liveness / readiness probe customisations (see groundhog2k/postgres docs).
  livenessProbe: {}
  readinessProbe: {}
  startupProbe: {}

# Key-Value Store / Redis Configuration
# Deploys Valkey via the official valkey-io/valkey sub-chart (image `docker.io/valkey/valkey`).
# For HA, point Langfuse at an external Redis/Valkey cluster (operator or managed service).
redis:
  # -- Deploy Valkey via the bundled valkey-io/valkey sub-chart. Disable to use an external Redis or Valkey.
  deploy: true

  # -- Redis host to connect to. If redis.deploy is true, this will be set automatically based on the release name.
  host: ""
  # -- Redis port to connect to.
  port: 6379

  # Redis TLS configuration
  tls:
    # -- Enable TLS for Langfuse↔Redis. When deploy is true, also enables TLS on the Valkey sub-chart.
    enabled: false
    # -- Path to the CA certificate file for TLS verification (mounted into Langfuse pods).
    caPath: ""
    # -- Path to the client certificate file for mutual TLS authentication.
    certPath: ""
    # -- Path to the client private key file for mutual TLS authentication.
    keyPath: ""

  # Authentication configuration. This block is dual-purpose:
  #   1. Langfuse-facing — `username`, `password`, `existingSecret`, `existingSecretPasswordKey`,
  #      `database` are consumed by the chart helpers to build REDIS_PASSWORD and the connection
  #      string for the langfuse pods.
  #   2. Sub-chart pass-through — `enabled`, `usersExistingSecret`, `aclUsers`, `aclConfig` are
  #      consumed by the bundled valkey-io/valkey sub-chart (when `redis.deploy: true`). The
  #      defaults below enable ACL auth, point Valkey at the chart-managed Secret
  #      (`<release>-redis-auth`, generated by templates/redis-secret.yaml), and grant the
  #      `default` user full permissions for the Langfuse application.
  auth:
    # -- Enable ACL-based authentication on the bundled Valkey sub-chart.
    enabled: true
    # -- Redis username Langfuse authenticates with. Set null to omit from the connection string; created as an ACL user when deploy is true.
    username: "default"
    # -- Redis password Langfuse authenticates with. Leave empty to auto-generate; set null to disable auth. URL-encode special characters.
    password: ""
    # -- Existing Secret holding the Redis password. When set, auth.password is ignored and the chart-managed Secret is not created.
    existingSecret: ""
    # -- The key in the existing secret that contains the password.
    existingSecretPasswordKey: ""

    database: 0

    # -- Valkey ACL users Secret (one key per username). Single source of truth for the chart-managed Secret's name — the chart creates the Secret under this exact name, so any release name works. Override when running two releases in one namespace.
    usersExistingSecret: "langfuse-redis-auth"
    # -- Valkey ACL users to create. Must include "default" when auth.enabled is true; `~* &* +@all` grants full access.
    aclUsers:
      default:
        permissions: "~* &* +@all"
    # -- Pass-through to valkey-helm: extra inline ACL config appended after `aclUsers`.
    aclConfig: ""

  # Redis Cluster configuration
  cluster:
    # -- Set to `true` to enable Redis Cluster mode. When enabled, you must set `redis.deploy` to `false` and provide cluster nodes.
    enabled: false
    # -- List of Redis cluster nodes in the format "host:port". Example: ["redis-1:6379", "redis-2:6379", "redis-3:6379"]
    nodes: []

  # Redis Sentinel configuration
  sentinel:
    # -- Set to `true` to enable Redis Sentinel mode. Cannot be enabled simultaneously with cluster mode. When enabled, you must set `redis.deploy` to `false`.
    enabled: false
    # -- Name of the Redis Sentinel master. Required when `redis.sentinel.enabled` is `true`.
    masterName: ""
    # -- Comma-separated list of Redis Sentinel nodes in the format "host:port". Example: "sentinel-1:26379,sentinel-2:26379,sentinel-3:26379". Required when `redis.sentinel.enabled` is `true`.
    nodes: ""
    # -- Username for Redis Sentinel authentication (optional).
    username: ""
    # -- Password for Redis Sentinel authentication (optional).
    password: ""
    # -- If you want to use an existing secret for the sentinel password, set the name of the secret here. (`redis.sentinel.password` will be ignored and picked up from this secret).
    existingSecret: ""
    # -- The key in the existing secret that contains the sentinel password.
    existingSecretPasswordKey: ""

  # Sub-chart settings (valkey-io/valkey) — passed through to the sub-chart.
  # See https://github.com/valkey-io/valkey-helm for the full schema.
  image:
    registry: docker.io
    repository: valkey/valkey
    tag: "8.0"
    pullPolicy: IfNotPresent

  service:
    type: ClusterIP
    # -- Port the primary Valkey Service exposes.
    port: 6379

  # -- Valkey replication. Default standalone; set enabled and replicas for primary/replica (Langfuse always targets the primary).
  replica:
    enabled: false
    replicas: 0
    persistence:
      size: 8Gi
      storageClass: ""
      accessModes:
        - ReadWriteOnce
    service:
      enabled: true
      type: ClusterIP
      port: 6379

  # -- Persistence for the primary node.
  dataStorage:
    enabled: true
    requestedSize: 8Gi
    className: ""
    accessModes:
      - ReadWriteOnce
    keepPvc: false

  # -- Pass-through to valkey-helm: contents of valkey.conf. This is the sub-chart's only config mechanism (it consumes no command-line flag values). Langfuse requires `maxmemory-policy noeviction` to avoid losing job data — validated at render time.
  valkeyConfig: |
    maxmemory-policy noeviction

  # -- Resource limits/requests for the Valkey pods.
  resources: {}

  podSecurityContext:
    fsGroup: 1000
    runAsUser: 1000
    runAsGroup: 1000
    seccompProfile:
      type: RuntimeDefault

  metrics:
    # -- Enable the bundled Valkey Prometheus exporter sidecar.
    enabled: false

# ClickHouse Configuration
# Deploys ClickHouse via the upstream ClickHouse/clickhouse-operator (apiVersion
# `clickhouse.com/v1alpha1`). The operator is alpha-quality at the time of writing — review
# release notes before upgrading and pin the operator chart version explicitly.
clickhouse:
  # -- Deploy ClickHouse (ClickHouseCluster + KeeperCluster CRs) via the operator. Disable to use an external or self-managed ClickHouse.
  deploy: true

  # -- ClickHouse hostname Langfuse connects to. Auto-set from the cluster Service when deploy is true; set explicitly for external ClickHouse.
  host: ""
  # -- HTTP port Langfuse uses to talk to ClickHouse.
  httpPort: 8123
  # -- Native TCP port Langfuse uses to talk to ClickHouse.
  nativePort: 9000
  # -- ClickHouse database name Langfuse connects to.
  database: default
  # -- Require ClickHouse operator CRDs when deploy is true. Disable for offline helm template/GitOps diffs (or pass `--api-versions clickhouse.com/v1alpha1/ClickHouseCluster`).
  crdCheck: true

  # Authentication configuration (Langfuse-facing — these values are read by the chart's helpers
  # to build CLICKHOUSE_USER / CLICKHOUSE_PASSWORD for the langfuse pods).
  auth:
    # -- Username Langfuse uses to authenticate to ClickHouse.
    username: default
    # -- Password Langfuse uses to authenticate to ClickHouse. Leave empty to have the chart generate one.
    password: ""
    # -- Existing Secret holding the ClickHouse password. When set, auth.password is ignored.
    existingSecret: ""
    # -- Key within existingSecret that contains the ClickHouse password.
    existingSecretKey: ""

  # Migration configuration
  migration:
    # -- ClickHouse migration URL (native TCP). Leave empty to derive from host/port when deploy is true.
    url: ""
    # -- Use SSL for ClickHouse schema migrations.
    ssl: false
    # -- Run ClickHouse schema migrations automatically when Langfuse starts.
    autoMigrate: true

  # Sub-chart settings (rendered as ClickHouseCluster + KeeperCluster CRs by this chart)
  cluster:
    # -- Enable ON CLUSTER DDL in Langfuse (CLICKHOUSE_CLUSTER_ENABLED). Disable only for an external non-clustered ClickHouse.
    enabled: true
    # -- Number of ClickHouse replicas. 1 is a valid single-pod cluster; 2+ requires Keeper enabled.
    replicas: 1
    image:
      # -- ClickHouse server image repository.
      repository: clickhouse/clickhouse-server
      # -- ClickHouse server image tag. Keep aligned with the version recommended for Langfuse v4.
      tag: "26.4"
    storage:
      # -- Persistent volume size for each ClickHouse pod.
      size: 100Gi
      # -- StorageClass for ClickHouse PVCs. Leave empty to use the cluster default.
      className: ""
      accessModes:
        - ReadWriteOnce
    # -- CPU/memory for ClickHouse pods. Non-empty defaults avoid the operator's ~1Gi fallback that OOMs under load.
    resources:
      requests:
        cpu: "1"
        memory: 2Gi
      limits:
        memory: 4Gi
    # -- Node selector for ClickHouse pods.
    nodeSelector: {}
    # -- Tolerations for ClickHouse pods.
    tolerations: []
    # -- Affinity rules for ClickHouse pods.
    affinity: {}
    # -- PriorityClass for ClickHouse pods.
    priorityClassName: ""
    # -- Extra ClickHouse server settings applied under the `<clickhouse>` config section.
    settings: {}
    # -- Extra ClickHouse user profile settings mounted into users.xml.
    profileSettings: {}

  keeper:
    # -- Deploy a KeeperCluster alongside ClickHouse. Must stay enabled while clickhouse.deploy is true — the ClickHouseCluster CRD requires a keeperClusterRef even for a single replica. Run ClickHouse externally (clickhouse.deploy=false + clickhouse.host) to avoid Keeper.
    enabled: true
    # -- Keeper replica count (must be odd: 1, 3, or 5). Use 3 for production HA.
    replicas: 3
    image:
      # -- ClickHouse Keeper image repository.
      repository: clickhouse/clickhouse-keeper
      # -- ClickHouse Keeper image tag. Keep aligned with the ClickHouse server tag.
      tag: "26.4"
    storage:
      # -- Persistent volume size for each Keeper pod.
      size: 20Gi
      # -- StorageClass for Keeper PVCs. Leave empty to use the cluster default.
      className: ""
      accessModes:
        - ReadWriteOnce
    # -- CPU/memory requests and limits for Keeper pods.
    resources:
      requests:
        cpu: 250m
        memory: 256Mi
      limits:
        memory: 1Gi
    # -- Node selector for Keeper pods.
    nodeSelector: {}
    # -- Tolerations for Keeper pods.
    tolerations: []
    # -- Affinity rules for Keeper pods.
    affinity: {}
    # -- PriorityClass for Keeper pods.
    priorityClassName: ""

# S3 / Object Storage Configuration
# Deploys SeaweedFS in allInOne mode via the seaweedfs/seaweedfs sub-chart (image `chrislusf/seaweedfs`).
# For production, set `s3.deploy: false` and point Langfuse at S3 / Azure Blob / GCS.
s3:
  # -- Deploy SeaweedFS via the bundled seaweedfs sub-chart. Disable to use external S3-compatible storage.
  deploy: true

  # -- Object storage provider for Langfuse uploads: s3 (default), azure, or gcs.
  storageProvider: "s3"

  # -- Default S3 bucket for uploads (overridable per upload type). Auto-created on SeaweedFS when deploy is true.
  bucket: "langfuse"
  # -- S3 region for uploads (overridable per upload type).
  region: "auto"
  # -- S3 endpoint for uploads (overridable per upload type). Auto-set to the SeaweedFS Service when deploy is true.
  endpoint: ""
  # -- Whether to force path style on requests. Required for SeaweedFS / MinIO. Can be overridden per upload type.
  forcePathStyle: true
  # -- S3 accessKeyId to use for all uploads. Can be overridden per upload type.
  accessKeyId:
    value: ""
    secretKeyRef:
      name: ""
      key: ""
  # -- S3 secretAccessKey to use for all uploads. Can be overridden per upload type.
  secretAccessKey:
    value: ""
    secretKeyRef:
      name: ""
      key: ""

  # Google Cloud Storage Configuration (only used when storageProvider is 'gcs')
  gcs:
    # -- GCS credentials JSON (or secretKeyRef). Falls back to the pod's environment/service account credentials if unset.
    credentials:
      value: ""
      secretKeyRef:
        name: ""
        key: ""

  # S3 Concurrency Configuration
  concurrency:
    # -- Maximum number of concurrent read operations to S3. Defaults to 50.
    reads: 50
    # -- Maximum number of concurrent write operations to S3. Defaults to 50.
    writes: 50

  # Event Upload Configuration
  eventUpload:
    # -- S3 bucket to use for event uploads.
    bucket: ""
    # -- Prefix to use for event uploads within the bucket.
    prefix: ""
    # -- S3 region to use for event uploads.
    region: ""
    # -- S3 endpoint to use for event uploads.
    endpoint: ""
    # -- Whether to force path style on requests. Required for SeaweedFS / MinIO.
    forcePathStyle: Null
    # -- S3 accessKeyId to use for event uploads.
    accessKeyId:
      value: ""
      secretKeyRef:
        name: ""
        key: ""
    # -- S3 secretAccessKey to use for event uploads.
    secretAccessKey:
      value: ""
      secretKeyRef:
        name: ""
        key: ""

  # Batch Export Configuration
  batchExport:
    # -- Enable batch export.
    enabled: true
    # -- S3 bucket to use for batch exports.
    bucket: ""
    # -- Prefix to use for batch exports within the bucket.
    prefix: ""
    # -- S3 region to use for batch exports.
    region: ""
    # -- S3 endpoint to use for batch exports.
    endpoint: ""
    # -- Whether to force path style on requests. Required for SeaweedFS / MinIO.
    forcePathStyle: Null
    # -- S3 accessKeyId to use for batch exports.
    accessKeyId:
      value: ""
      secretKeyRef:
        name: ""
        key: ""
    # -- S3 secretAccessKey to use for batch exports.
    secretAccessKey:
      value: ""
      secretKeyRef:
        name: ""
        key: ""

  # Media Upload Configuration
  mediaUpload:
    # -- Enable media uploads.
    enabled: true
    # -- S3 bucket to use for media uploads.
    bucket: ""
    # -- Prefix to use for media uploads within the bucket.
    prefix: ""
    # -- S3 region to use for media uploads.
    region: ""
    # -- S3 endpoint to use for media uploads.
    endpoint: ""
    # -- Whether to force path style on requests. Required for SeaweedFS / MinIO.
    forcePathStyle: Null
    # -- S3 accessKeyId to use for media uploads.
    accessKeyId:
      value: ""
      secretKeyRef:
        name: ""
        key: ""
    # -- S3 secretAccessKey to use for media uploads.
    secretAccessKey:
      value: ""
      secretKeyRef:
        name: ""
        key: ""
    # -- Maximum content length for media uploads. Defaults to 1GB.
    maxContentLength: 1000000000
    # -- Expiry time for download URLs. Defaults to 1 hour.
    downloadUrlExpirySeconds: 3600

  # Authentication for the bundled SeaweedFS S3 gateway. The chart wires these credentials
  # into both Langfuse (as LANGFUSE_S3_*_ACCESS_KEY_ID / *_SECRET_ACCESS_KEY) and into a
  # generated SeaweedFS IAM config Secret consumed by the allInOne pod.
  auth:
    # -- Access key ID Langfuse uses to authenticate against the SeaweedFS S3 gateway.
    rootUser: "langfuse"
    # -- S3 secret access key Langfuse uses against the SeaweedFS gateway. Leave empty to have the chart generate one.
    rootPassword: ""
    # -- Existing Secret for S3 access/secret keys (via rootUserSecretKey / rootPasswordSecretKey).
    existingSecret: ""
    # -- Key in the existing secret that contains the S3 access key ID.
    rootUserSecretKey: ""
    # -- Key in the existing secret that contains the S3 secret access key.
    rootPasswordSecretKey: ""

  # -- Legacy default bucket list; first entry is used when neither s3.bucket nor a per-upload bucket is set.
  defaultBuckets: "langfuse"

  # Sub-chart settings (seaweedfs/seaweedfs). In allInOne mode, master/volume/filer/s3 are disabled in favor of one combined pod.
  master:
    enabled: false
  volume:
    enabled: false
  filer:
    enabled: false
  s3:
    enabled: false

  allInOne:
    enabled: true
    image:
      registry: docker.io
      repository: chrislusf/seaweedfs
      tag: "3.95"
      pullPolicy: IfNotPresent
    s3:
      enabled: true
      port: 8333
      enableAuth: true
      # -- SeaweedFS IAM config Secret. Single source of truth for the chart-managed Secret's name — the chart creates the Secret (from s3.auth) under this exact name, so any release name works. Override when running two releases in one namespace.
      existingConfigSecret: "langfuse-s3-auth"
      # -- Buckets to create by the SeaweedFS post-install/post-upgrade hook. Must include every bucket Langfuse uses (`s3.bucket` and any per-upload-type overrides) — validated at render time.
      createBuckets:
        - name: langfuse
      createBucketsHook:
        resources: {}
    data:
      # -- Persistence for the SeaweedFS data directory.
      type: persistentVolumeClaim
      size: 50Gi
      storageClass: ""
      accessModes:
        - ReadWriteOnce
    service:
      type: ClusterIP
      internalTrafficPolicy: Cluster
    resources:
      limits:
        cpu: 2
        memory: 2Gi
      requests:
        cpu: 500m
        memory: 1Gi

# Additional manifests
extraManifests: []
